<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Panel - Job Portal</title>
    <link rel="stylesheet" href="styles.css">
    <style>
        /* Basic Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Body and Font */
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f4f7f6;
            color: #333;
            display: flex;
            height: 100vh;
            overflow-y: auto;
        }

        /* Sidebar Styling */
        .sidebar {
            width: 250px;
            background-color: #1abc9c;
            color: white;
            padding: 20px;
            position: fixed;
            height: 100%;
            transition: all 0.3s ease;
        }

        .sidebar h2 {
            text-align: center;
            margin-bottom: 30px;
            font-size: 24px;
        }

        .sidebar ul {
            list-style-type: none;
            padding: 0;
        }

        .sidebar ul li {
            margin-bottom: 20px;
        }

        .sidebar ul li a {
            color: white;
            text-decoration: none;
            font-size: 18px;
            display: block;
            padding: 10px;
            border-radius: 4px;
            position: relative;
            transition: background-color 0.3s ease, padding-left 0.3s ease;
        }

        .sidebar ul li a:hover,
        .sidebar ul li a.active {
            background-color: #16a085;
            padding-left: 20px;
        }

        .sidebar ul li a::before {
            content: '';
            position: absolute;
            left: 0;
            top: 50%;
            transform: translateY(-50%);
            width: 5px;
            height: 0;
            background-color: white;
            transition: height 0.3s ease;
        }

        .sidebar ul li a:hover::before,
        .sidebar ul li a.active::before {
            height: 100%;
        }

        /* Main Content Styling */
        .main-content {
            margin-left: 250px;
            padding: 40px;
            width: calc(100% - 250px);
            transition: margin-left 0.3s ease;
            overflow-y: auto;
        }

        header {
            background-color: #1abc9c;
            color: white;
            padding: 20px;
            text-align: center;
            border-radius: 5px;
            margin-bottom: 30px;
        }

        .content {
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            animation: fadeIn 1s ease-in-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        /* Form Styling */
        .job-form {
            background-color: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 500px;
            animation: slideIn 0.5s ease-in-out;
        }

        @keyframes slideIn {
            from {
                transform: translateY(50px);
                opacity: 0;
            }
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

        .form-group {
            margin-bottom: 20px;
            transition: all 0.3s ease;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: bold;
            color: #1abc9c;
        }

        .form-group input,
        .form-group textarea,
        .form-group select {
            width: 100%;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
            transition: border-color 0.3s ease;
        }

        .form-group input:focus,
        .form-group textarea:focus,
        .form-group select:focus {
            border-color: #1abc9c;
            outline: none;
            box-shadow: 0 0 5px rgba(26, 188, 156, 0.5);
        }

        .submit-btn {
            background-color: #1abc9c;
            color: white;
            border: none;
            padding: 15px 20px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.3s ease;
            width: 100%;
            font-size: 18px;
            font-weight: bold;
        }

        .submit-btn:hover {
            background-color: #16a085;
            transform: translateY(-3px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .submit-btn:active {
            transform: translateY(0);
            box-shadow: none;
        }

        /* Responsive Styling */
        @media (max-width: 768px) {
            .sidebar {
                width: 200px;
            }

            .main-content {
                margin-left: 200px;
                width: calc(100% - 200px);
            }
        }

        @media (max-width: 576px) {
            .sidebar {
                width: 180px;
            }

            .main-content {
                margin-left: 180px;
                width: calc(100% - 180px);
            }

            .sidebar ul li a {
                font-size: 16px;
            }
        }
    </style>
</head>
<body>
    <div class="sidebar">
        <h2>Admin Panel</h2>
        <ul>
            <li><a href="#dashboard" class="active">Dashboard</a></li>
            <li><a href="#add-job">Add Job</a></li>
            <li><a href="#candidates">Candidates</a></li>
            <li><a href="#employers">Employers</a></li>
            <li><a href="#reports">Reports</a></li>
            <li><a href="/">Home</a></li>
            <li><a href="/logout">Logout</a></li>
        </ul>
    </div>

    <div class="main-content">
        <header>
            <h1>Add Job Opportunity</h1>
        </header>

        <section id="add-job" class="content">
            <form class="job-form" method="post" action="/job-posting">
                <div class="form-group">
                    <label for="job-title">Job Title</label>
                    <input type="text" id="job-title" name="jobTitle" required>
                </div>
                <div class="form-group">
                    <label for="job-company">Company</label>
                    <input type="text" id="job-company" name="companyName" required>
                </div>
                <div class="form-group">
                    <label for="job-description">Job Description</label>
                    <textarea id="job-description" name="description" rows="4" required></textarea>
                </div>
                <div class="form-group">
                    <label for="job-requirements">Job Requirements</label>
                    <textarea id="job-requirements" name="skill" rows="4" required></textarea>
                </div>
                <div class="form-group">
                    <label for="job-location">Location</label>
                    <input type="text" id="job-location" name="location" required>
                </div>
                <div class="form-group">
                    <label for="job-type">Job Type</label>
                    <input type="text" name="jobType" placeholder="full-time/internship" >
                </div>
                <div class="form-group">
                    <label for="job-salary">Salary</label>
                    <input type="text" id="job-salary" name="salary" required>
                </div>
                <button type="submit" class="submit-btn">Add Job</button>
            </form>
        </section>
    </div>

    <script src="scripts.js"></script>
</body>
</html>