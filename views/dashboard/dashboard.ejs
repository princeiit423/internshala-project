<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Jobs Portal</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: white;
      overflow-x:hidden;
    }
    body::-webkit-scrollbar {
        width: 15px; /* Width of the scrollbar */
      }
      
      body::-webkit-scrollbar-track {
        background: #f1f1f1; /* Background color of the scrollbar track */
        border-radius: 10px; /* Rounding the track */
      }
      
      body::-webkit-scrollbar-thumb {
        background-color: #1d7756; /* Scrollbar color */
        border-radius: 10px; /* Rounding the scrollbar */
        border: 2px solid #f1f1f1; /* Adds a border for a better effect */
      }
      
      body::-webkit-scrollbar-thumb:hover {
        background-color: #155b3d; /* Darker color on hover */
      }


    .navbar {
        width: 100%;
        padding: 20px;
        background-color: #ffffff;
        box-shadow: 0px 2px 10px rgba(0, 0, 0, 0.1);
        display: flex;
        justify-content: space-between;
        align-items: center;
      }
  
      .navbar .logo {
        font-size: 24px;
        font-weight: bold;
        color: #2f7368;
        text-decoration: none;
      }
  
      .navbar .nav-links {
        display: flex;
        align-items: center;
        gap: 20px;
        
      }
  
      .navbar .nav-links a {
        color: white;
        text-decoration: none;
        font-weight: bold;
      }
  
      .navbar .post-job-btn {
        background-color: #2f7368;
        color: white;
        padding: 8px 16px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        font-weight: bold;
        text-decoration: none;
      }
  
  
    .container {
      max-width: 1200px;
      margin: 2rem auto;
      padding: 1rem;
    }
    .container h2{
        text-align:center;
        background-color:#F4F5F7;
        padding:2rem;
        font-weight:300;
        font-size:1.8rem;
        margin-bottom:2rem;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      background-color: #ffffff;
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }
    table th, table td {
      padding: 1rem;
      text-align: left;
      border-bottom: 1px solid #e1e1e1;
    }
    table th {
      
      color: #333;
    }
    table tr:last-child td {
      border-bottom: none;
    }
    .action-icons {
      display: flex;
      gap: 0.5rem;
    }
    .action-icons i {
      cursor: pointer;
      font-size: 1.2rem;
    }
    .view-icon { color: #1d7756; }
    .edit-icon { color: #007bff; }
    .delete-icon { color: #dc3545; }
    .footer {
      background-color: #1d7756;
      color: #ffffff;
      text-align: center;
      padding: 1rem;
      position: fixed;
      bottom: 0;
      width: 100%;
    }
  </style>
</head>
<body>

    <div class="navbar">
        <a href="#" class="logo">JobsPortal</a>
        <div class="nav-links">
          <a href="#" style=color:#2f7368;>Login</a>
          <a href="#" class="post-job-btn" style="margin-right:2rem;">Post a job</a>
        </div>
      </div>

  <div class="container">
    <h2>My Jobs List</h2>
   
    <table>
      <thead>
        <tr>
          <th>Title</th>
          <th>Job Type</th>
          <th>Posted Date</th>
          <th>Application Deadline</th>
          <th>Action</th>
        </tr>
      </thead>
      <tbody id="job-list">
        <tr>
          <td><%=user.applicantId.jobId.jobTitle %></td>
          <td><%=user.applicantId.jobId.jobType %></td>
          <td><%=user.applicantId.jobId.createdAt %></td>
          <td><%=user.applicantId.jobId.applicationDeadline %></td>
          <td class="action-icons">
            <i class="view-icon" onclick="viewJob()">👁️</i>
            <i class="edit-icon" onclick="editJob()">✏️</i>
            <i class="delete-icon" onclick="deleteJob(this)">🗑️</i>
          </td>
        </tr>
        <!-- Additional rows can go here -->
      </tbody>
    </table>
  </div>

  <div class="footer">
    © 2022 Jobs Portal. Designed by Your Name.
  </div>

  <script>
    function viewJob() {
      alert("Viewing job details...");
      // Add view functionality here
    }

    function editJob() {
      alert("Editing job...");
      // Add edit functionality here
    }

    function deleteJob(element) {
      if (confirm("Are you sure you want to delete this job?")) {
        let row = element.parentElement.parentElement;
        row.remove();
        alert("Job deleted successfully.");
      }
    }
  </script>

</body>
</html>
